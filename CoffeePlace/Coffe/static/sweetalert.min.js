// Accion boton productos
$(document).ready(function () {
  $(".btn-eliminar").click(function () {
    Swal.fire({
      title: 'Estas Seguro?',
      text: "No podrás revertir esto!",
      icon: 'warning',
      showCancelButton: true,
      confirmButtonColor: '#3085d6',
      cancelButtonColor: '#d33',
      confirmButtonText: 'Si, eliminalo!'
    }).then((result) => {
      if (result.isConfirmed) {
        window.location = "/delete/" + $(this).attr("prodid");
      }
    })
  });
});

// Accion boton Materias Primas
$(document).ready(function () {
  $(".btn-eliminar1").click(function () {
    Swal.fire({
      title: 'Estas Seguro?',
      text: "No podrás revertir esto!",
      icon: 'warning',
      showCancelButton: true,
      confirmButtonColor: '#3085d6',
      cancelButtonColor: '#d33',
      confirmButtonText: 'Si, eliminalo!'
    }).then((result) => {
      if (result.isConfirmed) {
        window.location = "/materiasprimas/delete/" + $(this).attr("materiaid");
      }
    })
  });
});

// Accion boton Proveedores
$(document).ready(function () {
  $(".btn-eliminar2").click(function () {
    Swal.fire({
      title: 'Estas Seguro?',
      text: "No podrás revertir esto!",
      icon: 'warning',
      showCancelButton: true,
      confirmButtonColor: '#3085d6',
      cancelButtonColor: '#d33',
      confirmButtonText: 'Si, eliminalo!'
    }).then((result) => {
      if (result.isConfirmed) {
        window.location = "/proveedores/delete/" + $(this).attr("proveedordid");
      }
    })
  });

});

// Accion boton Usuarios
$(document).ready(function () {
  $(".btn-eliminar3").click(function () {
    Swal.fire({
      title: 'Estas Seguro?',
      text: "No podrás revertir esto!",
      icon: 'warning',
      showCancelButton: true,
      confirmButtonColor: '#3085d6',
      cancelButtonColor: '#d33',
      confirmButtonText: 'Si, eliminalo!'
    }).then((result) => {
      if (result.isConfirmed) {
        window.location = "/usuarios/delete/" + $(this).attr("userdid");
      }
    })
  });

});



